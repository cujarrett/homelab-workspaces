apiVersion: platform.local.lab/v1alpha1
kind: Api
metadata:
  name: github-exporter
  namespace: github-exporter
spec:
  parameters:
    image: ghcr.io/cujarrett/github-exporter:sha-c25339e06d10221b2512c4d423d4f7de6094204f
    port: 8080
    # Serves metrics on the traffic port, so the default 9090 would scrape nothing.
    metricsPort: 8080
    consumes:
      - { host: api.github.com }
    configFrom:
      - github-exporter-config
    secretsFrom:
      - github-exporter-token
