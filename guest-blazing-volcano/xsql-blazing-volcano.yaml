apiVersion: platform.local.lab/v1alpha1
kind: XSql
metadata:
  name: xsql-blazing-volcano
spec:
  parameters:
    namespace: guest-blazing-volcano
    backend: private-cloud
    dataRetention: delete
