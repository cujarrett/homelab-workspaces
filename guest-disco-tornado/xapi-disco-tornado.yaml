apiVersion: platform.local.lab/v1alpha1
kind: XApi
metadata:
  name: xapi-disco-tornado
spec:
  parameters:
    namespace: guest-disco-tornado
    image: ghcr.io/cujarrett/hello-world-api:latest
    port: 8080
    replicas: 1
    host: demo3-api.mattjarrett.dev
    tlsIssuer: letsencrypt-prod
    sqlRef:
      name: xsql-disco-tornado
    cache:
      enabled: true
