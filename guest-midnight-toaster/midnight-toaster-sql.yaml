apiVersion: platform.local.lab/v1alpha1
kind: XSql
metadata:
  name: midnight-toaster-sql
spec:
  parameters:
    namespace: guest-midnight-toaster
    backend: private-cloud
    dataRetention: delete
