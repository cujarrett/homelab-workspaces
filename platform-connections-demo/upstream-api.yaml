apiVersion: platform.local.lab/v1alpha1
kind: Api
metadata:
  name: upstream-api
  namespace: platform-connections-demo
spec:
  parameters:
    image: ghcr.io/cujarrett/platform-connections-demo-api:sha-b6f4e4addcadc98401f0c855af5819a46bcae349
    repo: https://github.com/cujarrett/platform-connections-demo
    port: 8080
    replicas: 1
    # No host, so only meshed workloads that declare it can reach this API.
    provides:
      - name: Data.Read
        auth: workload
        allowedCallers:
          - { namespace: platform-connections-demo, app: authorized-api }
      # Offered, and held by nobody. Being let in is not the same as being allowed
      # to do everything once inside.
      - name: Data.Admin
        auth: workload
        allowedCallers: []
